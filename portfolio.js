let nav = document.querySelectorAll(".navmenu-parent")[0];
let navSmall = document.querySelectorAll(".navmenu-small")[0];

let typed = new Typed(".typed", {
    strings: ["Front-End Developer", ".NET Full-Stack Developer"],
    typeSpeed: 30,
    backSpeed: 30,
    loop: true,
  });
